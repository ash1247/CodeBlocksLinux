#include <bits/stdc++.h>

using namespace std;

int main()
{
    deque<int> deq;
    vector<string> svec;
    stack<int> stk(deq);
    stack<string, vector<string>> str_stk;
    stack<string, vector<string>> str_stk2(svec);
}
